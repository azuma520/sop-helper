{
  "name": "@aisop/workflows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\"",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "bullmq": "^5.63.0",
    "node-cron": "^4.2.1"
  },
  "devDependencies": {
    "vitest": "^4.0.6"
  }
}
